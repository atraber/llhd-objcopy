entity %bar (i32 %0) (i32 %1) {
    %2 = add i32 %0 9000
    %y = add i32 %0 42
}

entity @foo (i32 %0, i32 %2, i1$ %data_i) (i32 %1, i1$ %data_o, i1$ %floating_o) {
    %i_bar = inst %bar (%0) (%1)

    %internal = sig i1$

    drv %data_o %data_i
    drv %floating_o %internal
}
